import '../utils/logger.dart';

/// Stub for VoiceService linking to Gemini Live API via WebSockets.
/// Currently reserved for future implementation in phase 2.
class VoiceService {
  void startListening() {
    Logger.info('VoiceService.startListening() called - stub');
  }

  void stopListening() {
    Logger.info('VoiceService.stopListening() called - stub');
  }
}
